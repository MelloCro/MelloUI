--------------------------------------------------------------------------------
-- MelloUI - Custom bar textures
--
-- Drop .tga or .blp files (power-of-two sizes, e.g. 32x32 or 256x32) into
-- MelloUI\Media\Textures\  and list them here. New files need a full client
-- restart before the game can see them.
--
-- Example:
--   { name = "My Bar", file = "MyBar.tga" },
--------------------------------------------------------------------------------

MelloUI_CustomTextures = {
	-- { name = "My Bar", file = "MyBar.tga" },
}
