# CurseForge description

The project description on CurseForge (authors.curseforge.com > MelloUI > Description), kept
here as the source. The picture at the top is `docs/update-<version>.jpg`, made by
`Tools\make_presentation.py` from the screenshots in the News folder; upload it in the
description editor and remove the previous one.

---

The text on the site is `docs/curseforge-description.md` (the same relaxed description as the README's top half, in CurseForge's markdown).
