# Tools/cache and Tools/output moved OUT of the addon folder (2026-09-22): they
# are generated, gitignored and ~3.5 GB, and this folder gets backed up and
# synced. They live in ../MelloUI-BuildData now; Tools/paths.py finds them, and
# still finds them here if a machine never moved them.
Tools/cache/
Tools/output/
# Generated MP3s that Tools/build_voice_pack.py packages into the voice pack
Tools/pack_sources/
# Python
__pycache__/
*.pyc
# Never keys or local scratch
*.key
.elevenlabs*
.claude/
*.zip
CHANGELOG-release.md
# Third-party luacheck source used by Tools/lint/run_luacheck.py
Tools/lint/luacheck/
# Working notes for the assistant sessions (local paths, process notes): not for the public repository
docs/HANDOVER.md
docs/plans/
